import { NextResponse } from "next/server";
import { createReview, getRestaurant, listReviews } from "@/lib/content-repository";
import { getCurrentAccount } from "@/lib/session";

export async function GET(_request: Request, context: { params: Promise<{ id: string }> }) { const { id } = await context.params; if (!(await getRestaurant(id))) return NextResponse.json({ error: "Restaurant not found." }, { status: 404 }); return NextResponse.json({ reviews: await listReviews(id) }); }
export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { const account = await getCurrentAccount(); if (!account) return NextResponse.json({ error: "Authentication required." }, { status: 401 }); const { id } = await context.params; if (!(await getRestaurant(id))) return NextResponse.json({ error: "Restaurant not found." }, { status: 404 }); const body = await request.json() as Record<string, unknown>; const rating = typeof body.rating === "number" ? body.rating : Number(body.rating); const reviewBody = typeof body.body === "string" ? body.body.trim() : ""; if (!Number.isInteger(rating) || rating < 1 || rating > 5 || reviewBody.length < 10 || reviewBody.length > 2000) return NextResponse.json({ error: "Rating must be 1-5 and review text must be 10-2000 characters." }, { status: 400 }); try { return NextResponse.json({ review: await createReview({ userId: account.id, restaurantId: id, rating, body: reviewBody }) }, { status: 201 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to create review." }, { status: 409 }); } }
