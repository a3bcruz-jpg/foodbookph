"use client";

import { useMemo, useState } from "react";
import Link from "next/link";
import { Bell, Bookmark, ChevronRight, Compass, Heart, Home, MapPin, MessageCircle, Plus, Search, SlidersHorizontal, Star, Store, UserRound, Utensils, X } from "lucide-react";
import { currentUser, posts as seedPosts, restaurants } from "@/lib/data";
import type { Post } from "@/lib/data";

type View = "home" | "discover" | "restaurants" | "food" | "profile";

export function FoodBookApp() {
  const [view, setView] = useState<View>("home");
  const [query, setQuery] = useState("");
  const [posts, setPosts] = useState(seedPosts);
  const [following, setFollowing] = useState<string[]>(["r1"]);
  const [showComposer, setShowComposer] = useState(false);
  const [notice, setNotice] = useState("");

  const filteredRestaurants = useMemo(() => restaurants.filter((restaurant) => `${restaurant.name} ${restaurant.cuisine} ${restaurant.location}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const toggleLike = (id: string) => setPosts((current) => current.map((post) => post.id === id ? { ...post, liked: !post.liked, likes: post.likes + (post.liked ? -1 : 1) } : post));
  const toggleSave = (id: string) => setPosts((current) => current.map((post) => post.id === id ? { ...post, saved: !post.saved } : post));
  const toggleFollow = (id: string) => { setFollowing((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id]); setNotice(following.includes(id) ? "Restaurant removed from following" : "Now following this restaurant"); setTimeout(() => setNotice(""), 2500); };

  return <div className="app-shell">
    <header className="topbar"><div className="topbar-inner"><button className="brand" onClick={() => setView("home")}><span className="brand-mark">F</span><span>foodbook<span className="brand-accent">PH</span></span></button><div className="top-search"><Search size={17} /><input aria-label="Search restaurants, food, or places" value={query} onChange={(event) => { setQuery(event.target.value); setView("discover"); }} placeholder="Search places, dishes, people" /></div><div className="header-actions"><button className="icon-button" aria-label="Notifications" onClick={() => setNotice("You are all caught up") }><Bell size={19} /><span className="notification-dot" /></button><Link className="profile-chip" href="/profile"><span className="avatar avatar-small">{currentUser.initials}</span><span className="profile-chip-name">{currentUser.name}</span></Link></div></div></header>
    <main className="page-grid"><aside className="sidebar"><nav><NavButton icon={<Home size={19} />} label="Home" active={view === "home"} onClick={() => setView("home")} /><NavButton icon={<Compass size={19} />} label="Discover" active={view === "discover"} onClick={() => setView("discover")} /><NavButton icon={<Store size={19} />} label="Restaurants" active={view === "restaurants"} onClick={() => setView("restaurants")} /><NavButton icon={<Utensils size={19} />} label="Food feed" active={view === "food"} onClick={() => setView("food")} /><NavButton icon={<UserRound size={19} />} label="My profile" active={view === "profile"} onClick={() => setView("profile")} /></nav><div className="sidebar-rule" /><button className="owner-link" onClick={() => setNotice("Owner dashboard is ready for your restaurant account") }><span className="owner-icon"><Store size={17} /></span><span><strong>Own a restaurant?</strong><small>Claim or manage a place</small></span><ChevronRight size={16} /></button><div className="sidebar-foot">© 2026 FoodBookPH<br /><span>Made for the local food scene</span></div></aside><section className="content">{view === "home" && <HomeView posts={posts} toggleLike={toggleLike} toggleSave={toggleSave} onCompose={() => setShowComposer(true)} onViewRestaurants={() => setView("restaurants")} />} {view === "food" && <FeedView posts={posts} toggleLike={toggleLike} toggleSave={toggleSave} />} {(view === "discover" || view === "restaurants") && <DiscoverView restaurants={filteredRestaurants} query={query} following={following} toggleFollow={toggleFollow} onClear={() => setQuery("")} />} {view === "profile" && <ProfileView posts={posts} />} </section><aside className="right-rail"><div className="rail-card"><div className="rail-heading"><h3>Popular near you</h3><button onClick={() => setView("restaurants")}>See all</button></div>{restaurants.slice(0, 3).map((restaurant) => <RestaurantMini key={restaurant.id} restaurant={restaurant} />)}</div><div className="rail-card mustard-card"><span className="eyebrow">FOR RESTAURANT OWNERS</span><h3>Your place deserves a seat at the table.</h3><p>Claim your listing, share what is new, and meet the people who love your food.</p><button onClick={() => setNotice("Claim flow opened for restaurant owners")}>Claim a restaurant <ChevronRight size={15} /></button></div></aside></main><nav className="mobile-nav"><NavButton icon={<Home size={20} />} label="Home" active={view === "home"} onClick={() => setView("home")} /><NavButton icon={<Compass size={20} />} label="Discover" active={view === "discover"} onClick={() => setView("discover")} /><button className="mobile-add" onClick={() => setShowComposer(true)} aria-label="Create post"><Plus size={24} /></button><NavButton icon={<Utensils size={20} />} label="Food" active={view === "food"} onClick={() => setView("food")} /><NavButton icon={<UserRound size={20} />} label="Profile" active={view === "profile"} onClick={() => setView("profile")} /></nav>{showComposer && <Composer onClose={() => setShowComposer(false)} onPublish={(caption, image) => { const newPost: Post = { id: `p${Date.now()}`, author: currentUser.name, handle: currentUser.handle, avatar: currentUser.initials, time: "just now", caption, image, place: "Your food diary", likes: 0, comments: 0, liked: false, saved: false }; setPosts((current) => [newPost, ...current]); setShowComposer(false); setNotice("Your post is live"); setTimeout(() => setNotice(""), 2500); }} />}{notice && <div className="toast">{notice}</div>}</div>;
}

function NavButton({ icon, label, active, onClick }: { icon: React.ReactNode; label: string; active: boolean; onClick: () => void }) { return <button className={`nav-button ${active ? "active" : ""}`} onClick={onClick}>{icon}<span>{label}</span></button>; }
function HomeView({ posts, toggleLike, toggleSave, onCompose, onViewRestaurants }: { posts: Post[]; toggleLike: (id: string) => void; toggleSave: (id: string) => void; onCompose: () => void; onViewRestaurants: () => void }) { return <><div className="welcome-row"><div><span className="eyebrow">SATURDAY, SEPTEMBER 06</span><h1>Good food finds<br /><em>good company.</em></h1><p className="intro">See what your city is tasting today.</p></div><button className="primary-button" onClick={onCompose}><Plus size={17} /> Share a food find</button></div><section className="feature-band"><div><span className="eyebrow">WEEKEND EDIT</span><h2>Tables worth<br />traveling for</h2><p>Three spots our community is talking about this week.</p><button className="text-button" onClick={onViewRestaurants}>Explore the edit <ChevronRight size={15} /></button></div><div className="feature-image"><img src="https://images.unsplash.com/photo-1515003197210-e0cd71810b5f?auto=format&fit=crop&w=1000&q=85" alt="A colorful Filipino meal shared at a table" /><span className="image-label">Makati · 12 finds</span></div></section><FeedHeader title="From your food circle" action="View all" /><div className="feed-stack">{posts.slice(0, 2).map((post) => <PostCard key={post.id} post={post} toggleLike={toggleLike} toggleSave={toggleSave} />)}</div></>; }
function FeedView({ posts, toggleLike, toggleSave }: { posts: Post[]; toggleLike: (id: string) => void; toggleSave: (id: string) => void }) { return <><div className="section-heading"><div><span className="eyebrow">THE COMMUNITY TABLE</span><h1>Food feed</h1><p>Real plates, honest reviews, no gatekeeping.</p></div><button className="filter-button"><SlidersHorizontal size={16} /> Latest</button></div><div className="feed-stack">{posts.map((post) => <PostCard key={post.id} post={post} toggleLike={toggleLike} toggleSave={toggleSave} />)}</div></>; }
function FeedHeader({ title, action }: { title: string; action: string }) { return <div className="feed-header"><h2>{title}</h2><button>{action} <ChevronRight size={15} /></button></div>; }
function PostCard({ post, toggleLike, toggleSave }: { post: Post; toggleLike: (id: string) => void; toggleSave: (id: string) => void }) { return <article className="post-card"><div className="post-top"><div className="post-author"><span className="avatar">{post.avatar}</span><span><strong>{post.author}</strong><small>{post.handle} · {post.time}</small></span></div><button className="more-button" aria-label="More options">•••</button></div><p className="post-caption">{post.caption}</p><div className="post-photo"><img src={post.image} alt={post.caption} /><span className="place-tag"><MapPin size={13} /> {post.place}</span></div><div className="post-actions"><button className={post.liked ? "liked" : ""} onClick={() => toggleLike(post.id)}><Heart size={18} fill={post.liked ? "currentColor" : "none"} /> {post.likes}</button><button><MessageCircle size={18} /> {post.comments}</button><button className="save-action" onClick={() => toggleSave(post.id)} aria-label="Save post"><Bookmark size={18} fill={post.saved ? "currentColor" : "none"} /></button></div></article>; }
function DiscoverView({ restaurants: list, query, following, toggleFollow, onClear }: { restaurants: typeof restaurants; query: string; following: string[]; toggleFollow: (id: string) => void; onClear: () => void }) { return <><div className="section-heading discover-heading"><div><span className="eyebrow">PLACES TO TASTE</span><h1>{query ? `Results for “${query}”` : "Discover your next favorite"}</h1><p>Independent spots, neighborhood staples, and hidden gems.</p></div><button className="filter-button"><SlidersHorizontal size={16} /> Filters</button></div>{query && <button className="clear-search" onClick={onClear}><X size={14} /> Clear search</button>}<div className="restaurant-grid">{list.map((restaurant) => <RestaurantCard key={restaurant.id} restaurant={restaurant} following={following.includes(restaurant.id)} toggleFollow={toggleFollow} />)}{list.length === 0 && <div className="empty-state"><Search size={28} /><h3>No places found</h3><p>Try a city, cuisine, or restaurant name.</p></div>}</div></>; }
function RestaurantCard({ restaurant, following, toggleFollow }: { restaurant: typeof restaurants[number]; following: boolean; toggleFollow: (id: string) => void }) { return <article className="restaurant-card"><div className="restaurant-photo"><img src={restaurant.image} alt={restaurant.name} /><span className="price-pill">{restaurant.price}</span></div><div className="restaurant-details"><div className="restaurant-title"><div><h3>{restaurant.name}</h3><p>{restaurant.cuisine}</p></div><span className="rating"><Star size={14} fill="currentColor" /> {restaurant.rating}</span></div><div className="restaurant-meta"><MapPin size={14} /> {restaurant.location} <span>·</span> {restaurant.reviews} reviews</div><div className="tag-row">{restaurant.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><div className="restaurant-footer">{restaurant.claimed ? <span className="claimed"><span /> Claimed</span> : <span className="unclaimed">Unclaimed</span>}<button onClick={() => toggleFollow(restaurant.id)} className={following ? "following" : "follow-button"}>{following ? "Following" : "Follow"}</button></div></div></article>; }
function RestaurantMini({ restaurant }: { restaurant: typeof restaurants[number] }) { return <div className="mini-restaurant"><img src={restaurant.image} alt="" /><div><strong>{restaurant.name}</strong><small><Star size={12} fill="currentColor" /> {restaurant.rating} · {restaurant.cuisine}</small></div><ChevronRight size={15} /></div>; }
function ProfileView({ posts }: { posts: Post[] }) { return <><div className="profile-header"><span className="profile-avatar">AC</span><div className="profile-copy"><span className="eyebrow">FOODBOOK MEMBER</span><h1>{currentUser.name}</h1><p>{currentUser.handle} · Quezon City</p><button className="outline-button">Edit profile</button></div></div><div className="profile-stats"><div><strong>{currentUser.posts}</strong><span>posts</span></div><div><strong>{currentUser.reviews}</strong><span>reviews</span></div><div><strong>{currentUser.following}</strong><span>following</span></div></div><FeedHeader title="Your latest finds" action="Review history" /><div className="profile-post-grid">{posts.map((post) => <img key={post.id} src={post.image} alt={post.caption} />)}</div></>; }
function Composer({ onClose, onPublish }: { onClose: () => void; onPublish: (caption: string, image: string) => void }) { const [caption, setCaption] = useState(""); const [image, setImage] = useState("https://images.unsplash.com/photo-1547592180-85f173990554?auto=format&fit=crop&w=1200&q=85"); return <div className="modal-backdrop" onClick={onClose}><div className="composer" onClick={(event) => event.stopPropagation()}><div className="composer-head"><h2>Share a food find</h2><button onClick={onClose} aria-label="Close"><X size={19} /></button></div><textarea value={caption} onChange={(event) => setCaption(event.target.value)} placeholder="What did you eat, and would you go back?" autoFocus /><div className="upload-preview"><img src={image} alt="Selected food preview" /><button onClick={() => setImage("https://images.unsplash.com/photo-1509440159596-0249088772ff?auto=format&fit=crop&w=1200&q=85")}><Plus size={16} /> Change photo</button></div><div className="composer-foot"><span>{caption.length}/280</span><button className="primary-button" disabled={!caption.trim()} onClick={() => onPublish(caption, image)}>Publish post</button></div></div></div>; }
